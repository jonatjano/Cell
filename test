GIT_STATUS=$(git status --porcelain=v2)

while IFS= read -r line 
do 
	MODIFIED_FLAG=$(echo $line | cut -d ' ' -f 2) 

	if [ $MODIFIED_FLAG = "M." ] 
	then
		FILE_NAME=$(echo $line | cut -d ' ' -f 9)
		FILE_EXT=$()
		npx eslint $FILE_NAME
	fi
done <<< "$GIT_STATUS"

